{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["BROWSER_CHANNEL", "HEADLESS", "NEXT_PUBLIC_API_URL", "API_URL"],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**",
        ".next/**"
      ]
    },
    "lint": {
      "outputs": []
    },
    "fmt": {
      "cache": false
    },
    "start": {
      "cache": true
    },
    "dev": {
      "cache": false
    },
    "watch": {
      "cache": false
    }
  }
}
